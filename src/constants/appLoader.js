/** Must match the loader timeout in App.jsx so hero intro and loader stay in sync. */
export const APP_LOADER_DURATION_MS = 3500;

/** Pause after loader hides before hero intro motion starts (lets the screen settle). */
export const HERO_POST_LOADER_DELAY_MS = 550;
